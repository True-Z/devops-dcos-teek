# 理解记录（docs 编写角色）

## 文档信息

- 更新时间：`2026-03-19`
- 记录方式：仅记录“可长期复用”的协作共识。

## 记录

- 时间：`2026-03-19`
  - 用户原始意图：将文档协作角色固定为“与用户共同编写 docs，并维护一致性”；主要编写点由用户指定。
  - 理解结论：后续以 `docs` 编写与一致性治理为主职责，优先复用现有检查脚本，不越界沉淀功能开发规则。

- 时间：`2026-03-19`
  - 用户原始意图：所有修改操作先提示，用户同意后处理。
  - 理解结论：默认先给改动方案，确认后再落盘，改完反馈结果与检查信息。

- 时间：`2026-03-19`
  - 用户原始意图：将沉淀经验以 md 文件长期保存，参考 `web-ele/AI` 的组织方式。
  - 理解结论：在仓库根新增 `AI-docs` 规则目录，聚焦 docs 角色协作，不照搬功能开发规则。

- 时间：`2026-03-19`
  - 用户原始意图：`docs` 下文档都应具备“精炼说明 + 可点击延伸阅读”的模式，部分正文也可直接加链接方便查看详情。
  - 理解结论：后续知识类文档默认补 `## 延伸阅读`，并在适合的位置增加内链/外链，保证内容既能快速浏览，也能顺着链接继续深入。

- 时间：`2026-03-19`
  - 用户原始意图：知识文档不能只讲逻辑，还要尽量给出实际可操作的最小示例；枚举项应优先表格化，术语可直接加外链说明。
  - 理解结论：后续知识说明类文档默认补“最小可执行示例”，对类型/策略/分类等内容优先使用表格，并为关键专业术语补充高质量外部链接。

- 时间：`2026-03-19`
  - 用户原始意图：后续会以 `问题：xxx` 的形式提问，希望回答后按现有格式与规范补到对应文档中。
  - 理解结论：默认把 `问题：xxx` 视为“问题驱动的文档补充指令”，先给出清晰答案，再落到最合适的 `docs` 文件并完成校验。

- 时间：`2026-03-19`
  - 用户原始意图：`问题：xxx` 这种提问在记录时可以保留这种形式梳理，但适合抽象成知识标题时，应使用规范标题，不直接照搬问题做标题。
  - 理解结论：后续记录问题驱动内容时，正文可保留“问题：xxx”语境；若进入正式知识结构，优先整理成稳定、可复用的标题表达。

- 时间：`2026-03-19`
  - 用户原始意图：进一步收紧问题驱动内容的落库规则，不能直接以 `问题：xxx` 或“问题解答”一类表达作为正式标题，并覆盖此前较弱的理解。
  - 理解结论：后续问题驱动内容进入正式 `docs` 时，必须转换成规范知识标题；提问语境只能放在正文说明中，不再作为标题表达。

- 时间：`2026-03-19`
  - 用户原始意图：前端模块要优先规范目录与文件命名，再持续补知识点；后续重点围绕 `CSS / JavaScript / Web / V8` 推进。
  - 理解结论：前端模块默认优先保证 `00.目录.md + 00.简介.md + 正文页` 结构完整，文件名需一眼看出用途，再按问题驱动补充具体知识。

- 时间：`2026-04-01`
  - 用户原始意图：在 docs 编写协作中减少 token 消耗，能通过移动、归位解决的问题就不要优先重写。
  - 理解结论：后续默认采用更节制的表达方式；对已有内容优先考虑移动、归位、重命名、重组等结构性调整，只有在无法满足语义、层级或一致性要求时才改写正文。

- 时间：`2026-04-01`
  - 用户原始意图：当进入 `plan mode` 时，只记录方案与结论，只读分析，不实施、不写代码、不修改文件；只有在 `Build mode` 明确说出“开始执行”后，才允许进入执行态。
  - 理解结论：后续将 `plan mode` 视为严格只读规划阶段；即使讨论已形成明确方案，也不会自动落盘，必须等用户明确切换到 `Build mode` 并发出“开始执行”指令后才实施。

- 时间：`2026-04-01`
  - 用户原始意图：如果没有明确指定，不要做会额外消耗 token 的行为，优先控制修改范围。
  - 理解结论：后续默认采用更省 token 的执行方式；若用户未明确要求联动清理、补链、顺手优化或范围扩展，则只完成当前目标所必需的最小步骤，不额外扩散修改。
