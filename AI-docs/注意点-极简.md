# 注意点-极简（docs 编写角色）

## 快速执行清单

- 先提修改方案，用户同意后再改。
- 仅改 `docs` 相关，保持最小改动。
- frontmatter 出现 `@xxx` 必须加引号。
- 新文档补全 `title/date/permalink/categories/tags`。
- 文档默认写成“精炼要点 + 延伸阅读”，必要时正文直接补链接。
- 知识类文档尽量补“最小示例”，枚举项优先表格化。
- 修改后执行 `pnpm docs:touch` 与 `pnpm docs:check`。
- 如涉及结构或渲染风险，再执行 `pnpm docs:build`。
