---
title: Npm 简介
date: 2026-03-20 09:41:05
permalink: /pm/npm-intro
categories:
  - 构建
  - 包管理器
  - Npm
tags:
  - Npm
  - 简介
---

[**Npm**](https://www.npmjs.com/)（Node Package Manager）,可译为“节点的包管理器”，由于现在 npm 是 Node.js 的一个组件，只要在网上下载 Node.js。并进行安装，即可得到 npm。

**技术文档**：

- Npm 操作手册：https://docs.npmjs.com/

## 基本配置

淘宝镜像

```sh
npm config set registry https://registry.npmmirror.com/
```

全局安装地址

```sh
npm config set prefix "D:\_System\Npm\Global"
```

全局缓存地址

```sh
npm config set cache "D:\_System\Npm\Cache"
```

## 最小使用路径

```sh
npm install
npm install axios
npm run dev
```

- 这条链路对应大多数 Node/前端项目的最小使用方式：安装依赖、添加依赖、执行脚本。

## 阅读建议

- 如果你只想先把 npm 用起来，优先看“最小使用路径”和“基本配置”。
- 如果你需要迁移环境，再重点看全局安装、缓存和 `PATH` 配置。
- 工具和插件部分适合按需回查，不必第一次就全部配置。

path设置

```sh
# new path
NODE_PATH：...Global/node_modules

# add path
...Global
```

## 插件

### Basis

| pluginName | Description |
| :--------: | :---------: |
|    nrm     | 下载源管理  |
|    pnpm    |  包管理器   |
|  prettier  | 代码格式化  |

## 工具

### nvm

[**nvm**](https://github.com/nvm-sh/nvm)（node.js version management）是一个 nodejs 的版本管理工具；是为了解决 node.js 各种版本存在不兼容现象，通过它可以安装和切换不同版本的 node.js。

- 下载：https://github.com/nvm-sh/nvm/releases

#### 配置文件修改

- filePath：C:\Users\True\AppData\Roaming\nvm\settings.txt

```txt
root: C:\Users\True\AppData\Roaming\nvm
path: C:\Program Files\nodejs
```

## 延伸阅读

- 仓库内相关： [包管理器简介](/pm-intro)、[pnpm 核心要点](/pm/pnpm-core-point)、[package.json 核心字段](/pm/package-json-core)
- 官方文档: [npm Docs](https://docs.npmjs.com/)
- 标准/规范: [package.json](https://docs.npmjs.com/cli/v11/configuring-npm/package-json)
